# pip install track for LYNX (fallback to the conda environment.yml).
#
# Python must be 3.9.5. The torch / PyTorch-Geometric wheels below are not on
# plain PyPI for CUDA builds — install them with the PyG find-links index that
# matches your torch + CUDA, e.g.:
#
#   pip install torch==2.3.1 torchvision==0.18.1
#   pip install torch-sparse==0.6.18 torch-geometric==2.6.1 \
#       -f https://data.pyg.org/whl/torch-2.3.1+cu121.html
#   pip install -r requirements.txt
#   pip install scFates==1.0.8 --no-deps   # stale pins; see note below
#   pip install -e .          # the `lynx` package itself
#
# Core
numpy>=1.23.5,<2.0
pandas>=2.1.0
scipy>=1.9.0
scikit-learn==1.3.2
scikit-image==0.22.0
matplotlib==3.9.4
seaborn==0.13.0
networkx==3.2.1
statsmodels==0.14.1
tifffile==2023.9.26

# Single-cell / spatial
scanpy>=1.10.1
squidpy==1.6.1

# scFates 1.0.8 declares an over-strict `pandas<2.0` (also numpy<2.0,
# matplotlib<3.9, networkx<3.0) that is mutually exclusive with squidpy's
# `pandas>=2.1.0`. The pins are stale — scFates 1.0.8 runs fine on pandas 2.x —
# so install it WITHOUT its dependency metadata, AFTER this file:
#
#   pip install -r requirements.txt
#   pip install scFates==1.0.8 --no-deps
#
# Its genuine runtime deps are listed here so `--no-deps` is safe:
elpigraph-python==0.3.1
simpleppt==1.1.4
plotly==5.22.0
adjustText==0.7.3
scikit-misc==0.3.1
python-igraph

# Deep learning (see note above for torch / PyG wheel install)
torch==2.3.1
torchvision==0.18.1
torchrl==0.4.0
torch-geometric==2.6.1
torch-sparse==0.6.18
pyro-ppl==1.9.1

# Utilities
jenkspy==0.4.1
ml-collections==0.1.1
py-pcha==0.1.3
opencv-contrib-python==4.9.0.80
opencv-python-headless==4.9.0.80
tqdm==4.66.1
wandb==0.19.8
